import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    padding: 20,
    backgroundColor: "seashell",
    borderWidth: 0,
    borderBottomWidth: 8,
    borderColor: "salmon"
  },
  headerItem: {},
});
