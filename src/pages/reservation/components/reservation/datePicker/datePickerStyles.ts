import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  web: {
    display: "flex",
    flexDirection: "column",
    flex: 1,
    alignItems: "stretch",
  },
  mobile: {
    display: "flex",
    flexDirection: "column",  
    alignItems: "center",
  },
});
