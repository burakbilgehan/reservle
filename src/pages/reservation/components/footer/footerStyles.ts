import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  header: {
    padding: 20,
    backgroundColor: "seashell",
    borderColor: "salmon",
    borderWidth: 0,
    borderTopWidth: 8,
  },
  headerItem: {},
});
