import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "salmon",
    alignItems: "center",
    padding: 10,
    // border
    borderRadius: 8,
    borderColor: "salmon",
    borderWidth: 8,
  },
  title: {
    color: "white",
  },
});
